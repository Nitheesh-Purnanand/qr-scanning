<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Details</title>
    <style>
        body {
            background: linear-gradient(135deg, #74ebd5, #9face6);
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .form-container {
            background: #fff;
            border-radius: 10px;
            padding: 40px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

        .form-container .heading {
            font-size: 2rem;
            color: #4a47a3;
            font-weight: bold;
            margin-bottom: 20px;
            text-align: center;
        }

        .form-container .heading-1 {
            font-size: 1.2rem;
            color: #333;
            margin: 10px 0 5px;
            font-weight: bold;
        }

        .form-container input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
        }

        .form-container button {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background: #4a47a3;
            color: #fff;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .form-container button:hover {
            background: #6c63ff;
        }

        .form-container .id-display {
            font-size: 1rem;
            color: #666;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <form action="" method="post">
            <div class="heading"><%= element.name %></div>
            <div class="heading-1">Email</div>
            <input type="text" name="email" value="<%= element.email %>">
            <div class="heading-1">Password</div>
            <input type="text" name="password" value="<%= element.password %>">
            <div class="id-display">ID: <%= element.id %></div>
            <br>
            <button type="submit">Update</button>
        </form>
    </div>
</body>
</html>
