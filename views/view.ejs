<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <style>
        body {
            background: linear-gradient(135deg, #0f2027, #203a43, #2c5364); /* Dark-themed gradient */
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #fff;
        }

        .details-container {
            background: #1f1f1f;
            border-radius: 10px;
            padding: 40px;
            width: 100%;
            max-width: 500px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
            text-align: center;
        }

        .details-container .heading {
            font-size: 2.5rem;
            color: #4caf50; /* Accent color for heading */
            font-weight: bold;
            margin-bottom: 20px;
        }

        .details-container .heading-1 {
            font-size: 1.5rem;
            color: #ccc;
            margin: 10px 0;
            font-weight: 500;
        }

        .details-container .value {
            color: #4caf50; /* Accent color for values */
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="details-container">
        <div class="heading"><%= element.name %></div>
        <div class="heading-1">Email: <span class="value"><%= element.email %></span></div>
        <div class="heading-1">Password: <span class="value"><%= element.password %></span></div>
        <div class="heading-1">ID: <span class="value"><%= element.id %></span></div>
    </div>
</body>
</html>
